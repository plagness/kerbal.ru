Original prompt: Всё ещё есть пустота

## 2026-07-23 — лишняя высота после установки

- Пользователь показал большой пустой участок между коротким состоянием выбора
  установки и следующим разделом каталога на живом сайте.
- Нужно измерить фактические границы блоков в окне 1237×660, убрать лишний
  вертикальный запас, сохранить привязку анимации Земля → Марс к DOM-якорям и
  проверить оба состояния установки.
- Измерение живого DOM: между `#install` и `#mission-end` только 54 px, но весь
  каталог (≈8070 px) имел `opacity: 0`. `IntersectionObserver` требовал 12%
  видимости — около 968 px, что невозможно при viewport 660 px.
- Порог появления снижен до 1% с нижним `rootMargin: -8%`: большие динамические
  секции теперь раскрываются сразу после входа в окно, короткие сохраняют мягкое
  появление.
- Локальная проверка во встроенном браузере: после перехода «Как установить»
  каталог находится на 453 px, получает `reveal in` и `opacity: 1`; в viewport
  сразу видны заголовок каталога и начало карточки. Ошибок/предупреждений
  консоли нет.
- Обязательный игровой Playwright-клиент пройден; стартовый кадр и
  `render_game_to_text` загружены без регрессий.
- После публикации повторено на `https://kerbal.ru/` при точном размере
  пользователя 1237×660: каталог начинается на 453 px, имеет `reveal in`,
  `opacity: 1` и виден в том же кадре сразу под установкой. Консоль чистая.

## 2026-07-23 — компактная команда в футере

- По комментариям пользователя отдельная секция «Команда проекта» и её
  поясняющий текст удалены.
- Участники из JSON продолжают рендериться автоматически, но теперь только как
  маленькие круглые GitHub-аватары, симметрично выровненные по центру футера
  непосредственно над ссылкой на репозиторий.
- Встроенный браузер, локальный футер: отдельные `#team` и `team.intro`
  отсутствуют, единственный участник отображается кругом 40×40, ряд центрирован,
  отступ до репозитория 15 px. Визуальная проверка пройдена, консоль чистая.
- Живая проверка `kerbal.ru` при 1237×660: центр аватара совпадает с центром
  рабочей области (дельта 0 px), размер 40×40, до репозитория 15 px; отдельной
  секции и вводного текста в DOM нет. GitHub Pages собран успешно.

## 2026-07-23 — основные моды, охват и заметные CTA

- В JSON отмечены 16 основных компонентов RP-1 через `featured: true`: ядро
  RO/RSS/RP-1, RealFuels/FAR/Kerbalism, ключевая надёжность и связь, основные
  наборы двигателей, баков, капсул и процедурных деталей.
- Каталог по умолчанию показывает «Основные» (16 из 83); «Все» раскрывает 83.
  Проверены также kerbal.ru: 36 и «Без RU»: 5 после учёта UI-перевода.
- `uiTranslation.inventoryAdditions` содержит 16 точных компонентов, впервые
  получивших русский через Harmony. Общий охват теперь 54 из 83 = 65% без
  двойного счёта уже русских модов.
- Кнопки вариантов установки получили контрастный CTA «Выбрать»/«Открыто»;
  оба сценария раскрывают правильные команды и по две кнопки копирования.
- Названия, категории и тизеры модов увеличены. Локально проверены состояния,
  desktop-композиция и консоль; ошибок нет.
- После публикации на `https://kerbal.ru/` повторно проверено: стартовый фильтр
  «Основные» показывает 16 из 83, «Все» — 83 из 83, общий охват — 65% и
  54 из 83. Оба варианта установки раскрываются, команды содержат `--ru-only`
  / `-RuOnly` для режима перевода, консоль чистая.
- Устаревшая CI-проверка `renderInventory(data)` заменена проверкой реально
  используемого единого каталога. Project checks и GitHub Pages завершились
  успешно на коммите `054a637`.

## 2026-07-23 — подробная установка для двух сценариев и четырёх платформ

- Найдена регрессия: при переходе на JSON две прежние пошаговые инструкции
  превратились в короткие карточки без процедуры установки самой сборки.
- Для «Полной автоматики» возвращён полный путь: чистая KSP 1.12.5, поиск игры,
  официальный CKAN, установка RP-1 Express Install / Graphics Low / RP-1 и
  последующая установка русского языка.
- Для «Только перевода» подробно показаны проверка версии, backup, копирование
  управляемых файлов и включение `LANGUAGE = ru`.
- У каждого сценария появились отдельные инструкции Windows, macOS, Linux и
  Steam Deck, а также раскрываемый ручной путь. Ссылка на ZIP строится из
  текущей версии релиза, поэтому не устаревает при следующем обновлении JSON.
- Вся структура (`flow`, `platforms`, `steps`, `manual`) хранится в
  `data/project.json`; валидатор проверяет четыре платформы, полноту шагов,
  ссылки и обязательные флаги установщиков.
- Во встроенном браузере проверены обе ветки, переключение на Steam Deck,
  ручная установка, динамическая ссылка `v26.4.zip` и кнопка COPY. Для
  Clipboard API добавлен резервный способ копирования; кнопка подтверждает
  успех состоянием `OK`. Ошибок и предупреждений консоли нет.

## 2026-07-23 — миссия растянута на всю страницу

- Причина слишком быстрого полёта: конец шкалы был привязан к верхней границе
  `#mission-end` (началу каталога), поэтому весь новый каталог и футер уже не
  участвовали в анимации.
- Единый прогресс Земли, Марса, камеры и ракеты теперь считается от
  `scrollY / (scrollHeight - innerHeight)`: верх страницы всегда `0`, последний
  доступный пиксель прокрутки всегда `1`.
- Убрано прежнее опережение ракеты, где первые 20% шкалы сразу превращались в
  30% траектории. Теперь используется один smoothstep на полном диапазоне.
- Высота документа пересчитывается через `ResizeObserver`: при раскрытии полной
  инструкции диапазон тестово вырос с 4951 до 5844 px, а прогресс автоматически
  остался нормализованным.
- Игровой Playwright-клиент подтвердил: 27% страницы → начало полёта (7% пути),
  55% → перелёт (31%), 82% → подход к Марсу (73%), низ страницы после
  сглаживания → 100% и `rocketLanded: true`. Стартовый, промежуточные и
  посадочный кадры визуально проверены; ошибок консоли нет.
