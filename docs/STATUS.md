# Статус сборки «Оператор» + перевода (живой)

Обновлено: 2026-07-24. Ветка `catalog-rework` (не запушена). Источник охвата — `translations/_index.json`.

## Сборка на Маке (играбельна)
- KSP 1.12.5 (ru), M1 Pro / 16 GB. GameData ~6.3 ГБ, 79 модов, 7376 MM-патчей.
- Загрузка ~6 мин (норм для тяжёлого пака). **Рантайм чистый** — во время игры 0 exception (все ошибки только на загрузке).
- Дерево техов заполнено (CTT-fill + Beamed Power + EPL). Перевод раскатан на 49 модов.

## Перевод — покрытие
- **105 модов, ~10 558 ключей** в библиотеке (`translations/`), 0 ошибок валидатора по 111 cfg.
- Методы: keyed (ru.cfg), mm-title (@PART), mm-contract (@CONTRACT_TYPE/@AGENT), ui-dict (словарь+движок), keyed-supplement (доборы).

## Известные проблемы (триаж)
### Наши — ИСПРАВЛЕНО (эффект на следующем запуске)
- ✅ **33 MM-ошибки на загрузке** (контракт/Strategia патчи): пробел в селекторе узла (`@AGENT[Frontier Link]`,
  `@EFFECT:HAS[#effectDescription[to milestone gains...]]`) → заменил на `?`. Коммит d58dca3, раскатано.

### Наши — В РАБОТЕ
- 🔧 **~18 CC-ошибок парсинга контрактов** (notes/description): пунктуация/em-dash вплотную к `@выражению`
  (`@/targetBody:`, `@biome.FullName() —`) ломает парсер Contract Configurator. Пере-перевод 4 паков со строгой
  защитой (пробел+слово после выражения) — воркфлоу идёт. Некритично (игра грузится), но эти контракты показывают
  сырой текст. После деплоя проверить лог.

### Пред-существующие (НЕ наши, некритичны — обычный шум больших паков)
- ReStock: `Cannot replace texture 'orange-jumbo-'` (18×) — косметика текстур.
- `Cannot find PartModule 'KethaneWetMassIndicator'` (16×) — мод ждёт Kethane (не ставим).
- US2: `Cannot clone model US_M_Wedge_ScienceBay` (13×) — модель-вариант US2.
- `Cannot find fx group for decoupler` (7×). Ни одна не ломает игру.

## Осталось доделать
### Перевод
- [~] Пере-перевод контрактов (CC-фикс) — воркфлоу идёт → валидировать grep-ом (0 примыканий), раскатать.
- [~] **ui-dict Haystack + RoutineMissionManager** — строки из DLL, движок ловит в рантайме. Воркфлоу идёт.
      🔴 Требует запуска игры для сверки (строки ловятся при отрисовке окна).
- [ ] kOS-терминал НЕ трогаем (язык программирования).

### Гит / тулинг / доки (миграция под новый концепт-хаб)
- [ ] `docs/UI-TRANSLATION.md`, `docs/MAINTAINING.md`, `docs/UPDATING.md` — дочистить RP-1-упоминания.
- [ ] `tools/sync_project_data.py` + `data/project.json` — стухли (инвентарь GameData); либо переориентировать на
      `_index.json`, либо убрать.
- [ ] `index.html` (сайт GitHub Pages) — подать `builds/_catalog.json` + библиотеку переводов.
- [ ] `AGENTS.md`, `CHANGELOG.md`, `progress.md` — синхронизировать; `perf/` (RP-1 declutter) — в архив.
- [ ] Windows-порт установщика (`install.ps1`) — старый удалён при реструктуре.

### Проверка (нужен твой запуск)
- [ ] Перезапуск → лог чистый от наших ошибок (MM + CC)?
- [ ] Дерево техов: все узлы наполнены?
- [ ] Русские контракты (созвездия/станции/спутники) читаются?
- [ ] Haystack/RMM окна на русском (после ui-dict деплоя)?

## Изменения сборки по фидбеку
- ❌ **KCT убран** (2026-07-24): оператор — «конструкционное время/планирование-на-таймер бесит», против «грузи-и-лети».
  Снят из `build.json.recommended` + удалён пресет (коммит 2bcc12a). Мод снять `ckan uninstall KerbalConstructionTime`
  когда игра закрыта (вотчер на PID ждёт закрытия); save-safe (нет деталей). Перевод оставлен в библиотеке.

## Разведка непереведённого (2026-07-24)
- 🎯 **~2638 англ. строк результатов экспериментов (ScienceDefs, литерал не #LOC)** — крупнейший пробел:
  DMagic 1681 (патчит стоковые seismicScan/gravityScan + свои 17 эксп.), Coatl 941 (GroundOps+зонды), US2 16.
  Метод: `@EXPERIMENT_DEFINITION:HAS[#id[X]]:FINAL { !RESULTS{} RESULTS{…ru…} }` (удалить+пересоздать, без индексов дублей).
  Чанкить по эксперименту. ПАРКОВАНО (переключился на KCT), вернуться.
- ✅ keyed-наука StationScience/SCANsat/Knes — через #LOC, покрыто нашими переводами.
- ⚪ агентства (Agents.cfg KAX/Coatl/AirplanePlus/Knes) — 1-2 литерал-строки каждый, мелочь.

## План работ (фазы, приоритет сверху)
Наблюдение: оператор в РАННЕЙ КАРЬЕРЕ (стартовые контракты, возврат аппаратов, ~4-6к фунтов).
Вижен: спутники → сеть покрытия → станции → автоматика (kOS). План ведёт перевод/контент под эти стадии.

**Ф0 — Добить корректность (в работе, блокирует чистый лог):**
- Пере-перевод 4 контракт-паков (CC-фикс: выражения не касаются пунктуации) → строгая валидация → деплой.
- Деплой ui-dict Haystack/RMM тем же заходом. Проверить лог после перезапуска оператора.

**Ф1 — Под текущую стадию (контракты читаются ПРЯМО СЕЙЧАС):**
- Контракт-паки + Strategia на русском (Ф0 закрывает). Стоковые контракты уже RU.
- Проверить, что стартовые «мировые рекорды»/исследования читаемы.

**Ф2 — Под «спутники → сеть» (следующая веха оператора):**
- Готово: CommNet Constellation, Antenna Helper, Constellations-контракты (после Ф0), JX2, Beamed Power.
- 🎯 НОВОЕ (уникальный контент под вижен): **русский kOS-гайд + стартовые .ks-скрипты** «автоматический вывод
  спутника на заданную орбиту / построение созвездия связи». kOS-язык не переводим, но ДОКУ и примеры — на русском.
  Это прямо цель оператора (планировать операции как код). Кандидат: `docs/guides/kos-satellites.ru.md` + `examples/`.

**Ф3 — Под «станции → глубокая автоматика»:**
- Готово: SSPX, Station Science, KerbinSpaceStation-контракты, Routine Mission Manager (ui-dict).
- Проверить RMM («слетал раз → автоповтор») — идеально под вижен; свериться в игре.

**Ф4 — Полиш проекта (концепт-хаб):**
- Дочистить доки (UI-TRANSLATION/MAINTAINING/UPDATING), tools/sync_project_data + data/project.json (→ _index.json),
  index.html (подать _catalog.json + библиотеку), AGENTS/CHANGELOG, perf/ → архив, Windows install.ps1.
- Библиотека для «Оператора» ~исчерпана → потом расширять на другие сборки каталога.

**Ф5 — Цикл проверки (нужны запуски оператора):**
- Каждый перезапуск: лог чист? дерево наполнено? контракты/детали RU? По мере прогресса — ловить новые пустые узлы дерева.
