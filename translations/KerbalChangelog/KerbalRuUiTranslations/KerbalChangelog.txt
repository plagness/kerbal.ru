# KerbalChangelog — намеренно ПУСТОЙ файл (0 строк перевода). Причина:
#
# Дизассемблирование IL (monodis) KerbalChangelog.dll показало, что мод не хардкодит
# английский текст в вызовах GUI.Label/GUILayout.Label/GUIContent/UI.Text — все 54 места
# отрисовки текста получают уже готовую строку через KSP.Localization.Localizer::Format(key),
# т.е. проходят через штатную систему локализации KSP, а не через сырые литералы.
#
# Этот мод — из числа FULL-модов (см. docs/MAINTAINING.md, §2): у него уже есть
# GameData/KerbalChangelog/Localization/en-us.cfg + наш собственный ru.cfg в этом же репозитории,
# который переводит все ключи (KerbalChangelog_mainTitle, _closeButtonCaption, _authorLabel и т.д.)
# напрямую через Localization{}-блок. Проверено: closeButtonCaption уже = "Закрыть".
#
# Добавлять сюда пары "Close<TAB>Закрыть" и т.п. бессмысленно: если LANGUAGE=ru и ru.cfg
# загружен, Localizer.Format УЖЕ возвращает русский текст до того, как он попадёт в GUI-вызов —
# Harmony-словарь его просто не увидит на английском для сравнения.
#
# Остальные короткие литералы в IL ("A","C","D","F","H","P","R","S") — однобуквенные коды
# типа изменения, сравниваются через string::Equals при парсинге .cfg, до GUI не доходят.
# Плюс служебные логи "[KCL] ..." — тоже не текст интерфейса, перевод не нужен.
