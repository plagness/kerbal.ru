# B9PartSwitch — второй проход. KerbalRuUiTranslator.cs получил патч на
# TMPro.TMP_Text.text (setter) — раньше физически не мог поймать ничего в этом моде
# (см. историю ниже), теперь часть диалоговых строк реально достижима. Перепроверено
# заново с нуля полным IL-дизассемблированием (monodis, независимая пересборка
# B9PartSwitch.dll + Assembly-CSharp.dll через MONO_PATH), не скопировано вслепую.
#
# Подтверждено по IL-трассировке (PopupDialog.SpawnPopupDialog(...) и
# MultiOptionDialog(name,msg,title,...) → PopupDialog читает MultiOptionDialog.title →
# DialogGUIBase.SetUpTextObject(TMPro.TextMeshProUGUI, string value, ...), первая
# инструкция которого — `text.set_text(value)` без изменений; DialogGUIButton.Create()
# использует тот же хелпер для текста кнопки):
#
# - FatalErrorHandler.UpsertDialog: title="B9PartSwitch - Fatal Error" (message —
#   string.Concat из списка ошибок, динамический, не входит), кнопка "Quit".
# - LockedSubtypeWarningHandler.UpsertDialog: title="B9PartSwitch - Locked Subtypes",
#   кнопка "OK".
# - SeriousWarningHandler.UpsertDialog: title="B9PartSwitch - Serious Warning",
#   кнопка "OK" (message динамический, как выше).
# - InstallChecker.Start(): PopupDialog.SpawnPopupDialog(..., title=
#   "Incorrect B9PartSwitch Installation", message=..., button="OK") — первый вызов (сток
#   GameData/B9PartSwitch); message динамический (Join путей). Второй похожий текст
#   ("Incorrect B9PartSwitch Installation: ...Plugins...") уходит ТОЛЬКО в Debug.Log,
#   никогда не в диалог — не включён.
#
# НЕ достижимо (перепроверено, всё ещё Local/config-driven, а не хардкод):
# - UIPartActionSubtypeSelector.switcherDescriptionText/subtypeTitleText — оба реально
#   вызывают TMP_Text.set_text(...), но аргумент — ModuleB9PartSwitch.switcherDescription /
#   PartSubtype.title / UIPartActionSubtypeButton.get_Title(), т.е. per-part/per-subtype
#   значение ИЗ КОНФИГА, разное у каждой детали, а не единый хардкод-литерал — глобальный
#   плоский словарь для этого не подходит. Эти значения резолвятся штатным KSP Localizer по
#   ключам #LOC_B9PartSwitch_*, для которых у мода уже есть полный ru.cfg (перевод Sooll3).
# - Заголовок модуля, "Объём", описания подтипов, аббревиатуры баков и т.д. — всё так же
#   через #LOC_B9PartSwitch_* до рендера, наш словарь их не увидит на английском.
#
# УТОЧНЕНИЕ (второй добор): «не достижимо» выше — про #LOC-значения (их резолвит Localizer).
# Но у части модов SUBTYPE.title / switcherDescription заданы ЧИСТЫМ АНГЛИЙСКИМ ЛИТЕРАЛОМ
# (без #LOC) — такие set_text отдаёт как есть, и словарь их ЛОВИТ. Собраны отдельно в
# B9PartSwitch-subtypes.txt (CoatlAerospace, SSPX «LF/Ox», NFA «Structural»). Движок грузит
# все *.txt из этой папки, так что оба файла работают вместе.
#
# Формат: Английский<TAB>Русский. Несовпадение = строка остаётся как есть.

B9PartSwitch - Fatal Error	B9PartSwitch — фатальная ошибка
Quit	Выйти
B9PartSwitch - Locked Subtypes	B9PartSwitch — заблокированные подтипы
B9PartSwitch - Serious Warning	B9PartSwitch — серьёзное предупреждение
Incorrect B9PartSwitch Installation	Неверная установка B9PartSwitch
OK	ОК

# --- История (первый проход, до появления патча TMP_Text.text) ---
# Полный грep по дизассемблированному B9PartSwitch.il тогда дал НОЛЬ совпадений на все
# точки перехвата: GUI.Label/Button/Box/Toggle/Window, GUILayout-аналоги, 4 конструктора
# GUIContent(string,...), UnityEngine.UI.Text.text, UnityEngine.UI.Dropdown.OptionData.text —
# мод их не вызывает вообще. Диалоги и UIPartActionSubtypeSelector рендерятся через
# TMPro.TextMeshProUGUI, который тогда не патчился. Остальные строки-кандидаты в IL —
# Debug.Log-диагностика, сообщения исключений, имена ConfigNode-ключей, имена шейдерных
# свойств, служебные имена трансформов/GameObject и строки с плейсхолдерами формата
# (string.Format/string.Concat — точного совпадения по построению никогда не будет).
