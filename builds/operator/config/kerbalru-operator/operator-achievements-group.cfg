// kerbal.ru «Оператор» — группа контрактов для «тихих» ачивок (operator-scanning-achievements.cfg,
// operator-station-achievements.cfg, operator-isru-achievement.cfg).
//
// БАГ (найден живым мониторингом v26.37, сразу на первом же реальном запуске после деплоя):
// все три файла ссылаются на `group = kerbalruOperatorAchievements`, но САМА группа нигде не была
// объявлена — ContractConfigurator требует явный узел CONTRACT_GROUP{name=...} для любой ссылки
// `group=`, иначе `ArgumentException: No contract group with name 'kerbalruOperatorAchievements'`
// на КАЖДЫЙ контракт (14 исключений подряд, по одному на каждую ачивку). :NEEDS[ContractConfigurator]
// — no-op, если мод не установлен.

CONTRACT_GROUP:NEEDS[ContractConfigurator]
{
	name = kerbalruOperatorAchievements
	displayName = Ачивки «Оператора»
	agent = Оператор
}
